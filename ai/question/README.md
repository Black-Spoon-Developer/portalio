## 질문 생성 기획
1. chat GPT API 연결
  : .md 파일 받았을 때 자동적으로 질문을 쭉 생성
  -> 일단은 CI/CD와 함께 연결 시키는 것에 초점을 둘려면 반드시 이 쪽이 V1으로 구현이 되어야함
2. 채용 관련 API 등을 통해 정보 모은 뒤 파인 튜닝 거친 모델
  : 직무, 채용공고에 적합한 질문 생성 가능할 것
  -> 질문은 문제가 없을텐데, 답변 쪽이 문제
  -> 질문 생성은 파인 튜닝한 모델로 답변 적절성 여부는 chat GPT API를 이용하는 방식을 사용해야할 것으로 보임